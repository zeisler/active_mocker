# _attributes.erb
<% attributes.each do |meth| -%>
  def <%= meth.name %>
    read_attribute(:<%= meth.name %>)
  end


  def <%= meth.name %>=(val)
    assign_attribute(:<%= meth.name %>, val, type: <%= meth.type %>)
  end
<% end -%>
